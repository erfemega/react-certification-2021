import styled from 'styled-components';

const VideoDetailWrapper = styled.div`
  display: flex;
`;

const PlayerWrapper = styled.div`
  width: 65vw;
`;

export { VideoDetailWrapper, PlayerWrapper };
